export function testModel() {
  // call the docker container with the model url and the dataset url
  // how to spin up a docker image in aws?
  // the docker will fetch the model and the dataset and will run the test
  // when the test is done, the docker will write the result in a file and database
}
